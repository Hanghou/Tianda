# 梓冠双延时线 — 开发指令

## 物理拓扑（方案B）

```
电脑 COM? ──── 延时线1 (ID=1)
电脑 COM? ──── 延时线2 (ID=2)
```

两个独立串口，各连一个 `QSerialPort`，互不干扰。实际串口号由用户配置。

---

## 开发步骤总览

| 步骤 | 内容 | 产出 |
|------|------|------|
| 1 | 协议帧构造/解析类 | `ZiguangProtocol` |
| 2 | 单设备串口通信类 | `DelayLineDevice` |
| 3 | 延时线控制页 UI | 两个对称 GroupBox |
| 4 | 实时位置轮询 | QTimer 100ms 发 0E 查询 |
| 5 | 设置按钮 + 归零按钮逻辑 | 发 04 / 07 命令 |

---

## 步骤一：协议帧封装

**目标**：纯数据转换类，不依赖硬件，可单元测试。

**7字节定长帧结构**：

```
字节0    字节1    字节2    字节3    字节4    字节5    字节6
 FC       ID      CMD      D[2]     D[1]     D[0]     FE
0xFC     1B       1B      ───── 3字节大端数据 ─────   0xFE
```

**延迟值换算规则**：

```
发送值 = 实际PS × 1000 → 拆3字节大端十六进制

例：200PS → 200×1000=200000 → 0x03 0x0D 0x40
    100PS → 100×1000=100000 → 0x01 0x86 0xA0
```

**需要封装的函数清单**：

### 帧构造（输入 → QByteArray）

| 函数 | 命令字节 | 数据域 | 说明 |
|------|---------|--------|------|
| `buildSetDelay(id, ps)` | `04` | ps×1000 | 设置绝对延迟值 |
| `buildHome(id)` | `07` | `00 00 00` | 归零 |
| `buildQueryPosition(id)` | `0E` | `00 00 00` | 查询当前位置 |
| `buildSetSpeed(id, speed)` | `02` | `00 00 speed` | speed: 0x12~0xFF |
| `buildStop(id)` | `0B` | `00 00 00` | 停止 |
| `buildSaveEprom(id)` | `0F` | `00 00 00` | 保存到EPROM |
| `buildEnableRealtime(id, on)` | `39` | `05 06 00/01` | 开/关实时上报 |

### 帧解析（QByteArray → 结构化数据）

| 函数 | 说明 |
|------|------|
| `parseResponse(frame7bytes)` | 校验 FC/FE → 提取 ID/CMD/DATA → 若 CMD=0xAA 则 rawToPs(DATA) |

**位置查询应答帧**：`FC ID AA XX XX XX FE`（CMD=0xAA 表示位置应答）

---

## 步骤二：串口通信封装

**目标**：每个延时线一个 `DelayLineDevice` 实例，封装 QSerialPort。

**串口参数**：9600 波特率 / 8 数据位 / 无校验 / 1 停止位 / 无流控

**每个实例持有的状态**：
- 设备 ID（1 或 2）
- 串口号（用户配置）
- 连接状态

**对外暴露的操作**：
- `open()` / `close()` — 打开关闭串口
- `setDelay(ps)` — 内部调 `buildSetDelay` → 写串口
- `goHome()` — 内部调 `buildHome` → 写串口
- `queryPosition()` — 内部调 `buildQueryPosition` → 写串口
- `stop()` — 内部调 `buildStop` → 写串口

**对外信号**：
- `positionUpdated(id, ps)` — 收到 0xAA 应答时发射
- `errorOccurred(msg)` — 通信异常时发射

**接收逻辑（onReadyRead）**：
1. 累积数据到内部 buffer
2. buffer 长度 ≥ 7 时，取前 7 字节
3. 校验 frame[0]==0xFC && frame[6]==0xFE
4. 不通过 → 丢弃 1 字节，重新找 FC
5. 通过 → 调 parseResponse，若 CMD==0xAA → emit positionUpdated
6. 移除已处理的 7 字节，继续检查

---

## 步骤三：UI 布局

**页面名**：延时线控制页（CARS 项目 Tab 之一）

**布局**：左右并列两个 QGroupBox

```
┌─ 延时线1 (ID=1) ──────────────────┐  ┌─ 延时线2 (ID=2) ──────────────────┐
│                                    │  │                                    │
│  延时值: [ QDoubleSpinBox ] PS     │  │  延时值: [ QDoubleSpinBox ] PS     │
│  实时位置: QLabel ("+200.000 PS")  │  │  实时位置: QLabel ("+150.500 PS")  │
│  状态: QLabel ("● 运动中" / "○ 停止")│  │  状态: QLabel ("● 运动中" / "○ 停止")│
│                                    │  │                                    │
│  [ 设置 ]  [ 归零 ]  [ 停止 ]      │  │  [ 设置 ]  [ 归零 ]  [ 停止 ]      │
│                                    │  │                                    │
└────────────────────────────────────┘  └────────────────────────────────────┘
```

**控件清单（每个延时线 ×2）**：

| 控件 | 类型 | 说明 |
|------|------|------|
| 延时值输入 | QDoubleSpinBox | 范围 0~最大量程，单位 PS，精度 0.001 |
| 实时位置 | QLabel | 大字显示，格式 `%+.3f PS`，每 100ms 刷新 |
| 状态指示 | QLabel | "● 运动中" 或 "○ 停止"，绿色/灰色 |
| 设置按钮 | QPushButton | 发 04 命令 |
| 归零按钮 | QPushButton | 发 07 命令 |
| 停止按钮 | QPushButton | 发 0B 命令 |

---

## 步骤四：实时位置刷新（方案A：轮询）

**机制**：一个 QTimer，100ms 间隔，串行查询两个设备。

**每个 tick 做的事**：

```
QTimer::timeout (每100ms)
  │
  ├─► device1->queryPosition()
  │     │
  │     └─► 串口写: FC 01 0E 00 00 00 FE
  │
  ├─► device2->queryPosition()    // 等 device1 发完再发
  │     │
  │     └─► 串口写: FC 02 0E 00 00 00 FE
  │
  └─► 两个设备各自 onReadyRead 收到 0xAA 应答
        │
        └─► emit positionUpdated(id, ps)
              │
              └─► UI 更新对应 QLabel + 状态指示
```

**注意事项**：
- 两个 `queryPosition` 不要并发发（虽然串口不同，但走同一 Timer 简单）
- 如果 500ms 没收到应答 → 状态变红 "通信超时"
- 位置变化 > 0 且在运动 → 状态显示 "● 运动中"
- 位置连续多次不变 → 状态显示 "○ 停止"

---

## 步骤五：设置按钮 + 归零按钮逻辑

### 设置按钮

```
用户输入 200.5 PS → 点 [设置]
  │
  ▼
① 校验输入合法（范围检查）
  │
  ▼
② device->setDelay(200.5)
  │
  ▼
③ ZiguangProtocol::buildSetDelay(id=1, ps=200.5)
  │  200.5 × 1000 = 200500
  │  200500 = 0x03 0x0F 0x34
  │
  ▼
④ 串口写: FC 01 04 03 0F 34 FE
  │
  ▼
⑤ 按钮反馈: 短暂变灰 200ms + 文字变 "已发送" → 恢复
  （不等到位，异步操作）
```

### 归零按钮

```
点 [归零]
  │
  ▼
① device->goHome()
  │
  ▼
② 串口写: FC 01 07 00 00 00 FE
  │
  ▼
③ 按钮变灰，文字变 "归零中..."
  │
  ▼
④ QTimer 持续轮询位置，直到读到 0.000 → 按钮恢复
```

### 停止按钮

```
点 [停止]
  │
  ▼
① device->stop()
  │
  ▼
② 串口写: FC 01 0B 00 00 00 FE
  │
  ▼
③ 状态立即变 "○ 停止"
```

---

## 协议命令速查卡

### 所有命令一览

| 操作 | 命令帧 | 说明 |
|------|--------|------|
| 设置延迟 | `FC [ID] 04 [D2] [D1] [D0] FE` | 延迟值×1000→3字节大端 |
| 归零 | `FC [ID] 07 00 00 00 FE` | 回到0位 |
| 查询位置 | `FC [ID] 0E 00 00 00 FE` | 返回应答帧 |
| 设置速度 | `FC [ID] 02 00 00 [SPD] FE` | SPD: 0x12最快 ~ 0xFF最慢 |
| 停止 | `FC [ID] 0B 00 00 00 FE` | 停在当前位置 |
| 增加步进 | `FC [ID] 05 [D2] [D1] [D0] FE` | 相对正向移动 |
| 减小步进 | `FC [ID] 06 [D2] [D1] [D0] FE` | 相对反向移动 |
| 循环起点 | `FC [ID] 08 [D2] [D1] [D0] FE` | 巡回模式起始值 |
| 循环终点 | `FC [ID] 09 [D2] [D1] [D0] FE` | 巡回模式终止值 |
| 循环停留 | `FC [ID] 0A 00 00 [SEC] FE` | 端点停留秒数 |
| 保存EPROM | `FC [ID] 0F 00 00 00 FE` | 不保存断电丢配置 |
| 开实时上报 | `FC [ID] 39 05 06 01 FE` | 设备主动推位置 |
| 关实时上报 | `FC [ID] 39 05 06 00 FE` | 停止推送 |
| 设置波特率 | `FC [ID] 01 [B2] [B1] [B0] FE` | 改后需重启 |
| 设置ID | `FC [ID] 38 05 06 [NEWID] FE` | 改设备ID |
| 格式化 | `FC [ID] 3A 05 06 [xx] FE` | 清空FLASH配置 |

### 位置应答帧

```
FC [ID] AA [D2] [D1] [D0] FE

D2/D1/D0 解析: 3字节大端 → 整数值 ÷ 1000 = 延迟值(PS)
```

### 延迟值换算示例

| PS | ×1000 | HEX(3字节) | 完整帧(设ID=1) |
|----|-------|-----------|----------------|
| 0 | 0 | 00 00 00 | FC 01 04 00 00 00 FE |
| 50 | 50000 | 00 C3 50 | FC 01 04 00 C3 50 FE |
| 100 | 100000 | 01 86 A0 | FC 01 04 01 86 A0 FE |
| 200 | 200000 | 03 0D 40 | FC 01 04 03 0D 40 FE |
| 330 | 330000 | 05 09 10 | FC 01 04 05 09 10 FE |
| 600 | 600000 | 09 27 C0 | FC 01 04 09 27 C0 FE |

---

## 开发顺序与验证点

```
① ZiguangProtocol 类
   → 验证: 构造帧对比手册例子，解析已知帧验证正确性
   → 不依赖硬件，纯单元测试

② DelayLineDevice 类  
   → 验证: 接一个真实延时线，发查询位置 → 看应答解析是否正确
   → 关键: 接收缓冲和帧对齐逻辑

③ 延时线1 UI + 逻辑
   → 验证: 归零按钮 → 看位置回到0；设延迟 → 看位置走到目标值
   → 先只做一个设备，跑通完整闭环

④ 延时线2 UI + 逻辑
   → 复制延时线1，改 ID 和串口号
   → 验证: 两个同时设不同延迟值，互不干扰

⑤ 双设备并发测试
   → 两个设备同时归零 → 同时设延迟 → 确认轮询不冲突
```

---

## 注意事项

1. **通电后必须先归零再设延迟** — 手册强制要求，UI 上可在首次设延迟前自动归零或禁掉设置按钮
2. **禁止热插拔** — 控制线带电插拔损坏设备，程序启动时就打开串口，退出时才关闭
3. **电源和信号走同一根 DB9** — 脚8/9是供电（+5V 2A），不是标准串口线，接线时注意
4. **速度值越小越快** — 0x12 最快，和直觉相反，UI 上标注清楚
5. **最大量程不要暴露给用户随意修改** — 设错会卡死电机
6. **配置修改后记得保存** — 速度/量程改了要发 0F 保存到 EPROM，否则断电丢失
7. **延迟值精度到 0.001PS** — QDoubleSpinBox 设 3 位小数
