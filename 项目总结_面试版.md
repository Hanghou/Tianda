# 多模块光学测量系统整合平台 - 面试总结

## 一、技术栈与框架

### 核心框架
| 类别 | 技术 | 版本 | 职责 |
|-----|------|------|------|
| UI框架 | Qt Widgets | 6.10.2 | 窗口、控件、布局、事件处理 |
| 图表库 | Qt Charts | 6.10.2 | 光谱曲线实时渲染（QChart/QLineSeries） |
| 串口通信 | Qt SerialPort | 6.10.2 | 光谱仪/延迟线/位移台/激光泵通信 |
| 网络通信 | Qt Network | 6.10.2 | 振镜控制卡TCP/UDP协议通信 |
| 语言 | C++ | C++17 | 核心业务逻辑 |
| 编译器 | MSVC | 2022 | Windows x64编译 |
| 构建工具 | qmake | - | 项目构建配置 |

### 项目架构设计
```
DeviceBase (抽象基类)
  ├── Spectrometer      // 光谱仪 — 串口协议
  ├── StageController   // 位移台 — 双串口聚合
  ├── DelayLine         // 延迟线 — 串口协议
  └── GalvoMirror       // 振镜控制卡 — TCP/UDP协议

SerialPortBase          // 串口通信基类（被上述设备组合使用）
```

---

## 二、核心业务功能

### 1. 多设备协同控制
- **设备类型**：2台光谱仪 + 4台激光泵 + 振镜控制卡 + 2台位移台 + 2台延迟线
- **通信方式**：串口（RS-232）+ TCP/UDP网络
- **核心流程**：预设参数 → 设备控制 → 数据采集 → 结果展示

### 2. 预设表格管理
- **振镜页**：光源功率预设表 + 延迟线预设表
- **位移台页**：波长调谐预设表 + 延迟线预设表
- **编辑方式**：统一"编辑"按钮弹出对话框，支持添加/删除行

### 3. 光谱数据采集
- **单次测量**：手动触发单帧采集
- **连续测量**：定时器循环采集，实时更新图表
- **数据可视化**：双路光谱曲线同步显示（FOPO路 + Stokes路）

### 4. 振镜打标控制
- **图形支持**：点、线、圆、多点阵列
- **控制指令**：开始/停止/暂停/继续打标

---

## 三、关键实现细节

### 1. 设备通信协议实现

#### OHLD四泵激光协议
```cpp
// 帧格式：0x68 + 设备ID(4B) + 控制字(1B) + 长度(2B) + 数据 + 校验 + 0x16
// 校验算法：设备ID~数据域累加取低8位后按位取反

QByteArray ohldBuildFrame(quint8 cmd, const QByteArray &data) {
    QByteArray frame;
    frame.append(0x68);                    // 帧头
    frame.append(4, 0x00);                 // 设备ID
    frame.append(cmd);                      // 控制字
    // ... 长度、数据、校验、帧尾
}
```

#### 振镜TCP/UDP协议
- **UDP 5998**：心跳检测（确认控制卡在线）
- **TCP 6002**：图形参数下发 + 寄存器读写
- **TCP 6003**：控制指令（'e'开始/'r'停止/'p'暂停/'c'继续）

### 2. 预设执行流程

```
用户点击"开始执行"
        │
        ▼
loadPowerPresetsFromTable()   // 从表格加载预设列表
        │
        ▼
智能设备状态检查               // 判断所需设备是否连接
        │
        ▼
executeCombinedPreset(index)   // 逐条执行预设
        │
        ├── 振镜跳转(scannerJump)
        ├── 设置激光泵功率(setOhldPumpCurrentSilent)
        ├── 设置延迟线(setDelay)
        └── 触发光谱采集(startScan)
        │
        ▼
定时器触发下一条预设执行
```

### 3. 编辑弹窗实现（关键设计）

```cpp
void showPowerPresetEditDialog() {
    QDialog *dialog = new QDialog(this);
    dialog->setWindowTitle("编辑光源功率预设");
    
    // 动态创建表格，结构与原表一致（无序号列）
    QTableWidget *tableWidget = new QTableWidget(dialog);
    
    // 从原表复制数据到弹窗
    for (int row = 0; row < ui->tableWidgetPowerPresets->rowCount(); ++row) {
        tableWidget->insertRow(row);
        // ... 逐列复制数据
    }
    
    // 保存按钮：写回原表时确保居中对齐
    QPushButton *btnSave = new QPushButton("保存", dialog);
    connect(btnSave, &QPushButton::clicked, [this, tableWidget, dialog]() {
        QTableWidget *sourceTable = ui->tableWidgetPowerPresets;
        sourceTable->setRowCount(0);
        for (int row = 0; row < tableWidget->rowCount(); ++row) {
            sourceTable->insertRow(row);
            for (int col = 0; col < tableWidget->columnCount(); ++col) {
                QTableWidgetItem *newItem = new QTableWidgetItem(item->text());
                newItem->setTextAlignment(Qt::AlignCenter);  // 关键：保持居中
                sourceTable->setItem(row, col, newItem);
            }
        }
        dialog->accept();
    });
}
```

---

## 四、项目难点与解决方案

### 难点1：多设备异构通信协议的统一管理

**问题**：5种设备使用不同通信方式（串口/TCP/UDP）和协议格式，代码易耦合。

**解决方案**：
- **分层抽象**：`DeviceBase`基类定义统一接口（connect/disconnect/isConnected/getDeviceInfo）
- **协议独立封装**：协议构建/解析函数放在独立的`protocol.h`文件中
- **组合优于继承**：OHLD四泵直接组合`SerialPortBase`实例

---

### 难点2：UI文件XML结构破坏

**问题**：直接修改`integration.ui`XML文件导致Qt Creator无法解析，界面空白。

**解决方案**：
- **最小修改原则**：仅通过Qt Designer添加/删除按钮
- **代码动态创建**：编辑弹窗完全用C++代码创建（QDialog + QTableWidget），避免XML依赖

---

### 难点3：双串口位移台同步控制

**问题**：两台位移台接在独立串口，需实现"双台同步位移"——两台都完成后才通知上层。

**解决方案**：
```cpp
// StageController内部维护两个串口实例
bool moveAbsoluteDual(double positionMm) {
    sendFrame1(buildFrame("ma", positionMm));  // 台1
    sendFrame2(buildFrame("ma", positionMm));  // 台2
    m_pendingMove1 = m_pendingMove2 = true;    // 跟踪响应
}

// 两台都收到PO响应后才发射信号
void checkMoveCompleted() {
    if (m_pendingMove1 && m_pendingMove2) {
        emit moveCompletedDual();
    }
}
```

---

### 难点4：编辑弹窗数据对齐问题

**问题**：保存数据到原表时，新创建的`QTableWidgetItem`默认左对齐，与原表居中不一致。

**解决方案**：在保存逻辑中显式设置对齐方式：
```cpp
newItem->setTextAlignment(Qt::AlignCenter);
```

---

### 难点5：Windows宏冲突

**问题**：Windows SDK的`min/max`宏与Qt Charts的`std::min/std::max`冲突。

**解决方案**：在头文件顶部定义`NOMINMAX`：
```cpp
#ifndef NOMINMAX
#define NOMINMAX
#endif
```

---

## 五、项目亮点

1. **模块化设计**：设备驱动独立封装，便于维护和扩展
2. **统一接口抽象**：`DeviceBase`基类提供一致的设备操作接口
3. **智能设备检测**：预设执行前自动检测所需设备连接状态
4. **动态UI创建**：编辑弹窗使用代码创建，避免XML结构问题
5. **双路同步显示**：两套光谱图表同步更新，便于对比分析

---

## 六、核心文件清单

| 文件 | 职责 | 关键内容 |
|------|------|----------|
| `UI/integration.cpp` | 主窗口实现 | 初始化、设备控制、预设执行、图表更新（~4100行） |
| `UI/integration.h` | 主窗口声明 | 槽函数、成员变量、辅助方法（~425行） |
| `GalvoMirror/galvo_mirror.h/cpp` | 振镜控制 | TCP/UDP通信、图形参数下发、打标控制 |
| `Spectrometer/spectrometer.h/cpp` | 光谱仪控制 | 串口通信、积分时间设置、数据采集 |
| `StageController/stage_controller.h/cpp` | 位移台控制 | 双串口聚合、同步位移 |
| `DelayLine/delay_line.h/cpp` | 延迟线控制 | 绝对延迟设置、归零操作 |
| `LaserDriver/ohld_protocol.h` | 激光泵协议 | 帧构建、响应解析 |
| `Utils/preset_manager.h/cpp` | 预设管理 | 预设数据结构定义 |

---

## 七、面试常见问题回答思路

### Q：如何处理多设备并发通信？
A：每个设备使用独立的串口/网络连接，通过Qt信号槽机制异步处理数据，避免阻塞UI线程。

### Q：预设执行过程中设备断开怎么办？
A：在执行循环中检查设备连接状态，若断开则暂停执行并提示用户，支持断点续执行。

### Q：为什么编辑弹窗用代码创建而非UI文件？
A：UI文件修改容易破坏XML结构，代码动态创建更灵活，且便于控制数据同步逻辑。

### Q：如何保证双位移台同步？
A：使用标志位跟踪两台设备的响应状态，只有两台都完成后才发射完成信号。

---

*Generated by Trae AI — 2026-05-20*